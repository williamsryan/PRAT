# Archived sample results

These files were generated by a superseded implementation and remain only as
historical debugging material. They do not satisfy the current provenance,
dynamic execution, exact removal, or post-removal verification checks.

Do not cite the counts or statuses in this directory. The current validator is
expected to reject them.

Generate current evidence with:

```bash
make compatibility-check
```

Each fresh target directory includes matching host/container run IDs, the exact
source commit, dynamic execution evidence for both builds, removal accounting,
post-removal verification, and a target-scoped validation report.
